# Root pyproject.toml - ruff, mypy, pytest, coverage, bandit config.
# This file's only purpose is tooling configuration; each tool under
# tools/ has its own pyproject.toml with build metadata.

[tool.ruff]
line-length = 110
target-version = "py310"
extend-exclude = [
    ".venv",
    "venv",
    "dist",
    "build",
    "*.egg-info",
    "_site",
    "examples/*/knowledge-graphs/*.json",
]

[tool.ruff.lint]
select = [
    "E",       # pycodestyle errors
    "F",       # pyflakes
    "I",       # isort
    "B",       # flake8-bugbear
    "UP",      # pyupgrade
    "W",       # pycodestyle warnings
    "S",       # bandit-equivalent security rules
    "C4",      # flake8-comprehensions
    "PIE",     # flake8-pie
    "RET",     # flake8-return
    "SIM",     # flake8-simplify
    "ASYNC",   # async best practices
    "RUF",     # ruff-specific
]
ignore = [
    "E501",    # line length - formatter handles
    "B008",    # function call in default arg (FastAPI Depends/Header)
    "B904",    # raise ... from ... - sometimes intentionally omitted
    "UP006",   # PEP 585 generics - not yet adopted repo-wide
    "UP007",   # PEP 604 unions - not yet adopted repo-wide
    "UP035",   # typing.Dict/List still used repo-wide
    "UP045",   # X | None - not yet adopted repo-wide
    "S101",    # assert in tests is fine
    "S104",    # binding 0.0.0.0 (uvicorn config)
    "S105",    # hard-coded password (in example .env strings)
    "S106",    # hard-coded password keyword arg (test fixtures)
    "S311",    # non-crypto random - we use it for canary IDs etc.
    "S603",    # subprocess without shell=False
    "S607",    # subprocess partial path (pg_dump, ollama)
    "RET504",  # unnecessary variable before return - style only
    "SIM108",  # ternary - style only
    "ASYNC240", # rule advises trio.Path / anyio.path - this codebase
                # is pure asyncio. The few startup-time os.path /
                # pathlib calls inside async fns happen once at
                # lifespan entry before the event loop starts serving,
                # so blocking is fine. Revisit if we ever adopt trio
                # or anyio.
]

[tool.ruff.lint.per-file-ignores]
"conformance/**/*.py" = ["E402", "B011", "S"]
"**/tests/**/*.py"    = ["E402", "B011", "F401", "S"]
"scripts/**/*.py"     = ["E402"]

[tool.ruff.format]
quote-style = "double"

# ---------------------------------------------------------------------
# mypy - strict on runtime packages, advisory on legacy CLI tools.
# ---------------------------------------------------------------------
[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
follow_imports = "silent"
warn_redundant_casts = true
warn_unused_ignores = true
warn_return_any = true
warn_unreachable = true
no_implicit_optional = true
files = [
    "reference-implementation/src/ski_model",
    "reference-implementation/src/symbolic_evaluator",
    "reference-implementation/src/tag_registry",
    "reference-implementation/src/telemetry_buffer",
    "tools/audit-ledger/src/audit_ledger/canonical.py",
    "tools/ski-sdk/src/ski_sdk",
    "conformance",
]
exclude = [
    "build/",
    "dist/",
    "\\.venv/",
    "_site/",
]

# Strict on the deterministic core (Symbolic Evaluator + telemetry
# buffer + tag registry + canonical hashing). Any change here ships
# with full type coverage.
[[tool.mypy.overrides]]
module = [
    "symbolic_evaluator",
    "symbolic_evaluator.*",
    "tag_registry",
    "tag_registry.*",
    "telemetry_buffer",
    "telemetry_buffer.*",
    "audit_ledger.canonical",
]
strict = true
disallow_untyped_defs = true
disallow_any_unimported = false
disallow_incomplete_defs = true

# Less strict on the FastAPI server and conformance test glue.
[[tool.mypy.overrides]]
module = [
    "ski_model",
    "ski_model.*",
    "conformance.*",
]
disallow_untyped_defs = false
check_untyped_defs = true

# ---------------------------------------------------------------------
# pytest
# ---------------------------------------------------------------------
# This is the single source of truth for pytest configuration. (The repo
# previously also carried a root ``pytest.ini`` whose settings silently
# shadowed this block - it dropped ``asyncio_mode`` and the conformance
# markers, leaving the full ``pytest`` run red. That file has been removed;
# ``conformance/pytest.ini`` remains so the conformance suite can also be
# run standalone via ``pytest conformance/``.)
[tool.pytest.ini_options]
minversion = "8.0"
testpaths = [
    "conformance",
    "tools/kg-extractor/tests",
    "tools/kg-validator/tests",
    "tools/ski-model-deploy/tests",
    "tools/audit-ledger/tests",
    "reference-implementation/src/symbolic_evaluator/tests",
    "reference-implementation/src/telemetry_buffer/tests",
    "reference-implementation/src/ski_model/v3/tests",
    "tools/ski-sdk/tests",
]
addopts = [
    "-ra",
    "--strict-markers",
    "--ignore=examples",
    "--ignore=reference-implementation/tls",
    "--ignore=_site",
]
markers = [
    "level1: Level 1 Foundational conformance tests",
    "level2: Level 2 Managed conformance tests",
    "level3: Level 3 Assured conformance tests",
    # Conformance-level markers (mirrored from conformance/pytest.ini so the
    # full root-level ``pytest`` run does not fail --strict-markers).
    "provenance: Level 1 - every verdict carries verifier-checked provenance",
    "durability: Level 2 - provenance is signed, reproducible, and audit-chained",
    "sovereignty: Level 3 - operable air-gapped, tamper-evident, end-to-end signed",
    "requires_live_deployment: Test requires a running SKI Model endpoint",
    "requires_ledger: Test requires a Postgres ledger DSN",
]
asyncio_mode = "auto"

# ---------------------------------------------------------------------
# coverage.py
# ---------------------------------------------------------------------
[tool.coverage.run]
source = [
    "reference-implementation/src",
    "tools",
]
omit = [
    "*/tests/*",
    "*/conftest.py",
    "examples/*",
    "_site/*",
]

[tool.coverage.report]
# Gating floor (measured 72% at 2026-06-10). Ratchet upward only.
fail_under = 70
exclude_lines = [
    "pragma: no cover",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]

# ---------------------------------------------------------------------
# bandit
# ---------------------------------------------------------------------
[tool.bandit]
exclude_dirs = ["tests", "examples", "conformance", "_site"]
skips = [
    "B101",   # assert_used - fine in tests/conformance
]
