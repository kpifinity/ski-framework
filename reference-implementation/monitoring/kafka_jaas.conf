// SASL/SCRAM JAAS configuration for the demo Kafka broker.
// Replace the placeholder credentials before any non-local use.
KafkaServer {
    org.apache.kafka.common.security.scram.ScramLoginModule required
    username="ski-kafka"
    password="CHANGE_ME";
};

Client {
    org.apache.kafka.common.security.scram.ScramLoginModule required
    username="ski-kafka"
    password="CHANGE_ME";
};
