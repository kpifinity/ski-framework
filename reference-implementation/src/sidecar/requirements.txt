# SKI Sidecar — pinned dependencies.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
python-dotenv==1.2.3
httpx==0.28.1
kafka-python==2.3.2
prometheus-client>=0.20,<1
# Explicit floors for transitive/base-image build tooling flagged by the
# container vulnerability scan (Trivy) independent of the app's own deps.
setuptools>=78.1.1
msgpack>=1.2.1
