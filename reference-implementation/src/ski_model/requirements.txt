# SKI Model — pinned dependencies.
# Update via pip-compile; do not loosen pins without a security review.
fastapi==0.110.0
uvicorn[standard]==0.27.1
pydantic==2.13.4
python-dotenv==1.0.1
psycopg[binary]==3.1.18
sqlalchemy==2.0.27
pyyaml==6.0.1
prometheus-client==0.20.0
httpx==0.27.0
cryptography==42.0.5
# Optional non-conformant demo backend. Remove this line in production builds
# to eliminate the possibility of cloud-API inference at build time.
anthropic==0.21.3
