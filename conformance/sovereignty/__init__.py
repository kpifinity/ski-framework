"""Sovereignty — Level 3 conformance (scaffold only).

Asserts that the runtime is operable air-gapped, the LLM transcript is
signed end-to-end, and tamper attempts on the ledger fail closed. These
tests are skipped pending the L3 infrastructure (network sandbox,
attestation flow). See ``docs/CONFORMANCE.md`` for the planned matrix.
"""
