# kg-extractor — pinned dependencies.
# Phase 1 (compilation) is permitted to use cloud LLMs but the
# implementation defaults to a local backend. PyPDF2 was replaced with
# pypdf due to CVE-2023-36464 and CVE-2022-24859.
pydantic==2.13.4
python-dotenv==1.2.1
click==8.1.8
pyyaml==6.0.3
httpx==0.28.1
pypdf==4.2.0
python-docx==1.2.0
# LLM backends — at least one is required at runtime; choose via
# KG_EXTRACTOR_LLM_BACKEND. anthropic and openai are for compilation
# only and must NEVER be used as the SKI Model runtime backend.
anthropic==0.104.1
openai==1.16.2
